﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿﻿# MoneyRobert Pro 系统评估与演进规划

> 文档版本：2.0  
> 创建日期：2026-06-22  
> 更新日期：2026-06-25  
> 代码基线：`main@27207dc`  
> 文档类型：系统评估、目标架构与演进设计

---

## 变更记录

### v2.0 (2026-06-25) 分级操作教程系列

| 变更项 | 文件 | 说明 |
|--------|------|------|
| 教程索引页 | `docs/操作盈利教程.md`（v1.0 → v2.0） | 将原单篇教程改造为四篇分级教程的索引页，含能力等级地图、学习路径建议（标准/快速/运维三条路径）、配套参考文档、系统核心能力速览 |
| 小白入门篇 | `docs/01-小白入门篇.md` | L0 等级教程（7 章 + 结业测试）：系统定位、Docker 部署、注册登录、仪表盘 4 卡片解读、行情页 5 区域解读（含市场状态与数据质量卡片）、设置页 4 区域（含 OKX API Key 录入与代理配置）、7 类常见基础问题 |
| 熟练操作篇 | `docs/02-熟练操作篇.md` | L1 等级教程（8 章 + 结业测试）：AI 综合分析 4 Tab、多 Agent 辩论 4 阶段、13 类 Agent 介绍、FundManagerDecision 8 字段解读、概率决策卡生成 8 步骤（含 v1.8 信任闭环字段）、回测创建与可信等级门禁、paper 模式自动交易循环逻辑、监控与紧急停止 |
| 资深调优篇 | `docs/03-资深调优篇.md` | L2 等级教程（10 章 + 结业测试）：辩论机制深度解析（4 阶段源码定位 + 动态可信度 + 基金经理风险否决）、风控三套默认值对比与调优（保守/平衡/激进三档）、概率校准（Brier Score + 线性校准公式）、EV/CVaR 模型详解（v1.8 增强版公式）、组合风控 4 项检查、晋级门禁 Level 0→3 详解、Demo→Live 完整实操、降级触发与恢复、决策调优台、数据库运维（备份/恢复/慢查询/索引） |
| 规则设计篇 | `docs/04-规则设计篇.md` | L3 等级教程（11 章 + 结业项目）：六层架构总览、修改风控规则（RiskEngine + PortfolioRiskEngine）、修改 Agent 行为（提示词/权重/新增 Agent）、修改决策引擎（EV/CVaR 公式/CalibrationData 扩展）、自定义特征与市场状态、添加决策卡字段 7 处同步路径、设计数据库迁移（安全原则 + 手动执行）、扩展 API 路由（REST 规范）、添加前端页面（Vue 3 + 路由 + 菜单 + API 封装）、测试与验证、重启与发布规范（含回滚策略） |

本次为纯文档新增，无代码变更，无需重启服务。教程系列覆盖从零部署到自主设计策略规则的完整能力成长路径，每篇设结业测试/项目，符合"操作轨迹可追踪"规则。

### v1.9 (2026-06-25) 用户操作盈利教程文档

| 变更项 | 文件 | 说明 |
|--------|------|------|
| 操作盈利教程 | `docs/操作盈利教程.md` | 新建用户操作手册，覆盖从部署到 live 实盘的完整盈利路径：系统部署（Docker Compose）、OKX API Key 录入、行情采集验证、多 Agent 辩论使用、概率决策卡生成与判定（EV/CVaR/数据新鲜度门禁）、回测可信等级门禁（display_only/comparable/promotion_eligible）、Paper→Demo→Live 晋级流程、风控参数配置建议、维护命令速查、API 速查表、盈利自检清单 |

本次为文档新增，无代码变更，无需重启服务。

### v1.8 (2026-06-23) EV/CVaR 模型增强 + 产品信任闭环

| 变更项 | 文件 | 说明 |
|--------|------|------|
| 概率校准接入决策引擎 | `backend/src/signals/decision_engine.rs` | 新增 `CalibrationData` 结构（Brier Score、校准斜率/截距、样本量）；`decide()` 在有可靠校准数据时将原始 confidence 校准为真实概率（线性校准：p = slope * p_raw + intercept） |
| 资金费率入账 | 同上 | `DecisionInput` 新增 `funding_rate_bps` 字段；成本计算从 `fee + slippage` 扩展为 `fee + slippage + funding`，按持仓周期累计资金费率成本 |
| CVaR 估算改进 | 同上 | 从 `1.65 * asset_vol * 5%` 改进为 `2.063 * sigma_daily * sqrt(holding_days) * position_pct`（正态分布 95% CVaR，考虑持仓周期的 sqrt(t) 效应） |
| DecisionCard 信任闭环字段 | `backend/src/signals/models.rs`、`backend/src/routes/signals_api.rs`、`backend/src/signals/store.rs` | DecisionCard 新增 `reasons`、`blockers`、`trust_level` 三个字段；API 请求/响应、DB INSERT/SELECT 全部同步更新 |
| 迁移补充 | `backend/migrations/018_quantitative_foundations.sql` | `decision_cards` 表新增 `reasons TEXT[]`、`blockers TEXT[]`、`trust_level VARCHAR(30)` 列；同时提供 `CREATE TABLE` 和 `ALTER TABLE ADD COLUMN IF NOT EXISTS` 兼容新旧部署 |
| 前端展示信任闭环 | `frontend/src/pages/DecisionCardPage.vue`、`frontend/src/api/index.ts` | 决策卡展示区新增决策原因列表、阻断原因列表、可信等级徽章（可晋级/可比较/仅展示）；生成表单新增信任闭环输入区 |

测试覆盖：新增 3 个单元测试（校准调整概率、不可靠校准跳过、资金费率增加成本），`cargo test --lib` 297 passed; 0 failed。

### v1.7 (2026-06-23) 架构收敛：Orchestrator + LedgerService + PortfolioRisk

本次更新解决文档中"下一步建议"的三项核心架构问题，将回测 runner 从"内联实现"收敛到"统一编排"：

| 变更项 | 文件 | 说明 |
|--------|------|------|
| Runner → DecisionOrchestrator | `backend/src/backtest/runner.rs` | 删除 runner 中直接调用 DecisionEngine/PositionSizingEngine/RiskEngine 的内联逻辑，统一走 `DecisionOrchestrator::orchestrate()`（信号→决策→仓位→风控→输出）。`handle_signal` 从 ~130 行缩减为 ~50 行编排调用 |
| Runner → LedgerService | `backend/src/backtest/runner.rs`、`backend/src/orchestration/ledger_service.rs` | `LedgerService` 新增 `apply_fill()` 方法（封装 `matching.apply_fill` + 记录 `LedgerEntry`）；runner 中所有成交入账（市价单/限价单/止损/强制平仓）统一走 `self.ledger.apply_fill()`，消除直接调用 `matching.apply_fill` |
| Portfolio Risk 接入主链路 | `backend/src/backtest/runner.rs` | 新增 `apply_portfolio_risk_check()` 方法：在单资产风控通过后，执行组合层面的 CVaR 预算、风险贡献集中度、相关性约束、流动性约束检查；不通过时按比例缩减仓位 |
| RiskEngine Clone | `backend/src/backtest/risk_engine.rs` | 为 `RiskEngine` 派生 `Clone`，支持 orchestrator 和 runner 共享风控配置 |
| Reports 迁移安全转换 | `backend/migrations/030_reports_schema_fix.sql` | 使用 PL/pgSQL `try_jsonb()` 函数安全转换 TEXT→JSONB：合法 JSON 直接解析，非法内容包装为 `{"raw": "..."}` 避免迁移失败 |

测试覆盖：`cargo check` 通过，`cargo test --lib` 294 passed; 0 failed。

架构收敛前后对比：

| 层级 | 收敛前（v1.6） | 收敛后（v1.7） |
|------|----------------|----------------|
| 决策流水线 | runner 内联 DecisionEngine + PositionSizingEngine + RiskEngine | runner → DecisionOrchestrator.orchestrate() |
| 交易执行 | runner 直接调 MatchingEngine.apply_fill() | runner → LedgerService.apply_fill() → MatchingEngine |
| 组合风控 | 未接入主链路 | runner → apply_portfolio_risk_check() → PortfolioRiskEngine.check() |
| 回测/模拟盘/实盘 | 各自直接调底层引擎 | 统一走 Orchestrator + LedgerService |

### v1.6 (2026-06-23) 回测中心前端页面

本次更新将后端回测可信等级能力接入前端，补全 P0 项"回测可信等级门禁"的端到端体验：

| 变更项 | 文件 | 说明 |
|--------|------|------|
| Backtest API 封装 | `frontend/src/api/index.ts` | 新增 `BacktestJobSummary`、`BacktestJobDetail`、`BacktestReport`、`TrustLevelResponse`、`CreateBacktestJobRequest` 类型与 `BacktestApi` 类（listJobs、getJob、createJob、startJob、getReport、getTrustLevel、assessTrustLevel） |
| 回测中心页面 | `frontend/src/pages/BacktestPage.vue` | 新建页面：左侧任务列表（状态徽章、收益、Sharpe）；右侧任务详情含三区域——任务配置（标的、时间、费用、滑点、杠杆）、绩效报告（总收益、Sharpe、回撤、胜率、盈亏比等 8 项指标）、可信等级门禁（三级等级徽章、6 项门禁检查、晋级资格、评估指标） |
| 路由注册 | `frontend/src/router/index.ts` | 新增 `/backtest` 路由指向 `BacktestPage.vue` |
| 侧边栏菜单 | `frontend/src/layouts/DashboardLayout.vue` | "量化信号"分组新增"回测中心"菜单项（FlaskConical 图标） |
| 类型修复 | `frontend/src/pages/SettingsPage.vue` | 修复 `AiProvider` 接口缺少 `name` 字段的预存类型错误 |

验证：`npm run build` 成功，`BacktestPage` 产物 16.73 kB（gzip 4.43 kB）。

### v1.5 (2026-06-23) 行情页接入市场状态与数据质量

本次更新增强行情分析页，将特征仓库的市场状态识别与数据质量能力接入前端：

| 变更项 | 文件 | 说明 |
|--------|------|------|
| 市场状态展示 | `frontend/src/pages/MarketPage.vue` | 接入 `GET /features/regimes/latest/{symbol}`，在行情页顶部新增市场状态卡片：显示状态徽章（趋势/震荡/高波动/突破）、置信度、ADX、波动分位 |
| 数据质量展示 | 同上 | 接入 `GET /features/quality`，新增数据质量卡片：显示质量等级徽章（优秀/良好/一般/较差）、覆盖率、缺口率、新鲜度 |
| 联动刷新 | 同上 | symbol 切换与刷新按钮同时触发市场状态、数据质量、K线、资金费率等数据加载 |

验证：`npm run build` 成功，`MarketPage` 产物 195.23 kB（gzip 62.85 kB）。

### v1.4 (2026-06-23) 概率决策卡前端页面

本次更新启动第四阶段"可信产品体验"，将后端概率决策卡能力接入前端：

| 变更项 | 文件 | 说明 |
|--------|------|------|
| Signal API 封装 | `frontend/src/api/index.ts` | 新增 `CreateDecisionCardRequest`、`DecisionCardResponse`、`CalibrationResponse` 类型定义与 `SignalApi` 类（createDecisionCard、listDecisionCards、getCalibration、computeCalibration） |
| 决策卡页面 | `frontend/src/pages/DecisionCardPage.vue` | 新建页面：左侧生成表单（概率分布、EV、CVaR、仓位建议、模型版本、数据新鲜度）；右侧最新决策卡展示（建议动作徽章、概率分布可视化条、关键指标网格、收益分位数、失效条件、数据血缘）；底部历史决策卡列表 |
| 路由注册 | `frontend/src/router/index.ts` | 新增 `/signals/decision-card` 路由指向 `DecisionCardPage.vue` |
| 侧边栏菜单 | `frontend/src/layouts/DashboardLayout.vue` | 新增"量化信号"分组与"概率决策卡"菜单项（Layers 图标） |

验证：`npm run build` 成功，`DecisionCardPage` 产物 20.35 kB（gzip 5.73 kB）；`vue-tsc --noEmit` 类型检查通过。

### v1.3 (2026-06-23) 测试门禁与数据质量 API

本次更新完成 P0 剩余项（agents 测试门禁）与 P1 项（数据质量查询 API）：

| 变更项 | 文件 | 说明 |
|--------|------|------|
| Agents 测试门禁 | `backend/tests/agents.rs`、`backend/tests/agents/*.rs` | 核验确认 6 个子测试模块（autonomous/debate/decision/promotion/risk/simulation）已通过 `#[path=...]` 接入 `agents.rs` 测试入口；22 个测试全部通过，无 HTML 转义或 async/await 缺陷；`cargo test --test agents` 可作为门禁命令 |
| 数据质量查询 API | `backend/src/routes/features_api.rs` | 新增 `GET /features/quality` 端点：未传时间范围时返回最新一条快照；传入 `start_time`/`end_time` 时返回历史快照列表；响应包含 freshness、gap_ratio、outlier_ratio、coverage_ratio、quality_grade |

测试覆盖：`cargo test --lib` 112 个通过；`cargo test --test agents` 22 个通过；合计 134 个测试全部通过。

### v1.2 (2026-06-23) 量化基础扩展

本次更新依据本规划与《deep-research-report.md》的 P0 优先级项，完成以下实现：

| 变更项 | 文件 | 说明 |
|--------|------|------|
| 滑点成本入账 | `backend/src/backtest/models.rs`、`matching_engine.rs`、`runner.rs` | `SimulatedFill` 新增 `slippage_cost` 字段，每笔成交保存滑点成本金额；绩效报告使用保存的金额而非临时计算 |
| Alpha/Beta 基准支持 | `backend/src/backtest/performance_engine.rs` | 新增 `compute_report_with_benchmark` 方法，支持传入基准收益序列计算真实 Alpha/Beta |
| 特征血缘 | `backend/migrations/018_quantitative_foundations.sql`、`backend/src/features/store.rs` | 新增 `feature_lineage` 表与 `FeatureLineage` 结构，记录数据源、计算版本、参数 hash |
| 数据质量快照 | 同上 | 新增 `data_quality_snapshots` 表与 `DataQualitySnapshot`、`QualityGrade` 结构，记录 freshness、缺口率、异常值率、覆盖率 |
| 概率决策卡 | `backend/src/signals/` 新模块、`backend/src/routes/signals_api.rs` | 新增 `signal_predictions`、`decision_cards` 表；实现 `DecisionCard`、`DecisionCardBuilder`、`SignalPrediction` 结构；提供 `POST /signals/decision-card` 等 API |
| 概率校准 | `backend/src/signals/calibration.rs` | 实现 Brier Score、Log Loss、校准曲线、校准误差计算；新增 `signal_calibration_reports` 表 |
| 回测可信等级 | `backend/src/backtest/trust_engine.rs`、`backend/src/routes/backtest_api.rs` | 新增 `backtest_trust_assessments` 表；实现三级门禁（display_only/comparable/promotion_eligible）；提供 `GET/POST /backtest/jobs/{job_id}/trust-level` API |
| 风控决策日志 | `backend/migrations/018_quantitative_foundations.sql` | 新增 `risk_decisions` 表，记录风控门禁结果与拒绝原因 |
| 执行审计日志 | 同上 | 新增 `execution_audit_logs` 表，记录订单执行全链路审计 |
| 绩效报告扩展 | 同上 | `performance_reports` 表新增 `alpha`、`beta`、`var_95`、`cvar_95` 列 |

测试覆盖：新增 15 个单元测试（signals 模块 15 个、trust_engine 6 个、matching_engine 滑点成本 1 个），全部通过。

---

## 一、总体结论

MoneyRobert Pro 已具备“加密资产智能分析与交易平台”的完整产品骨架：行情、技术分析、衍生品数据、多 Agent 辩论、模拟盘、回测、自动交易、风险确认、记忆和自进化均已有覆盖。

当前系统更准确的定位是：**AI 辅助交易工作台 + 确定性风控执行系统**，尚不是已经通过统计检验的量化预测与稳定盈利系统。

主要差距不在页面或 Agent 数量，而在四个基础能力：

1. 回测账本、撮合、费用、滑点和权益曲线必须持续用测试证明正确。
2. Agent 置信度必须从主观评分升级为可校准概率、净期望和历史样本表现。
3. 数据管线必须覆盖长期历史、特征版本、市场状态和数据血缘。
4. 模拟盘、回测和实盘必须共享组合、风控和执行语义。

本次核验后，原文档中的第一阶段不应再笼统标记为“全部完成”。最新代码已经修复或启动了多项关键工作，但仍存在部分设计与实现缺口。

---

## 二、最新代码完成度核验

| 领域 | 代码位置 | 当前状态 | 结论 |
|------|----------|----------|------|
| 撮合与账户记账 | `backend/src/backtest/matching_engine.rs` | 已实现反向平仓、反手剩余数量开新方向仓位、开仓/加仓/平仓/反手单元测试 | 第一阶段核心问题基本完成，仍需补复杂序列和杠杆保证金测试 |
| 绩效指标 | `backend/src/backtest/performance_engine.rs` | 已改为日末权益计算 Sharpe，新增 Sortino 与 Calmar | 部分完成；滑点成本仍返回 `0.0`，Alpha/Beta、CVaR、尾部风险、回撤持续期仍缺失 |
| 报告数据隔离 | `backend/src/routes/reports.rs` | 查询、统计、创建、更新、删除均按 `user_id` 过滤 | API 层已完成；数据库 RLS 可作为后续增强 |
| Agent 测试接入 | `backend/tests/agents.rs` 与 `backend/tests/agents/*.rs` | 6 个子模块已通过 `#[path=...]` 接入；22 个测试全部通过，无 HTML 转义或 async 缺陷 | 已完成 (v1.3) |
| 特征仓库 | `backend/migrations/017_feature_store_and_regime.sql`、`backend/src/features/*` | 新增特征定义、特征值、市场状态、日 OHLCV 聚合表及 API | 第二阶段已启动；仍缺长期回填、数据质量、特征血缘和概率校准 |
| 市场状态识别 | `backend/src/features/regime.rs`、`backend/src/routes/features_api.rs` | 已提供五类状态识别与查询接口 | 可作为 MVP；还不是经过样本外验证的状态模型 |
| 路由挂载 | `backend/src/routes/mod.rs` | `/features` 已挂载 | 已完成 |

### 完成度判断

第一阶段“可信底座”已完成约 65%。已完成撮合反手修复、基础资金守恒测试、报告 API 用户隔离、核心绩效指标初步修复；仍缺绩效指标完整性、测试覆盖、滑点成本、回测结果使用门禁。

第二阶段“量化基础”已完成约 25%。已完成特征仓库与市场状态的表结构、基础计算模块、查询 API；仍缺长期历史回填、订单簿/逐笔/清算/基差/跨交易所数据、Walk-forward 验证、概率校准。

---

## 三、目标设计原则

系统应保留模块化单体，不必立即微服务化，但必须建立清晰六层：

```text
Market Data -> Feature Store -> Model/Signal -> Portfolio/Risk -> Execution -> Attribution
```

| 层级 | 职责 | 不应承担的职责 |
|------|------|----------------|
| Market Data | 采集、清洗、去重、时间对齐、质量标记 | 直接生成交易建议 |
| Feature Store | 计算、存储和版本化特征 | 临时拼接不可复现特征 |
| Model/Signal | 输出概率、分位数、状态和解释 | 直接决定仓位或下单 |
| Portfolio/Risk | 组合约束、仓位预算、风险门禁 | 修改模型概率 |
| Execution | 幂等下单、撮合、订单状态机、审计 | 绕过风控执行 |
| Attribution | 绩效、归因、校准、复盘 | 反向污染训练样本 |

核心约束：

1. LLM/Agent 只能生成候选信号、证据摘要和解释，不直接拥有实盘执行权。
2. 风控和订单执行必须由确定性代码完成。
3. 每次决策必须记录数据版本、特征版本、模型版本、规则版本和证据时间。
4. 回测、模拟盘、影子模式和实盘共享同一套撮合、费用、滑点和仓位语义。
5. 所有收益展示默认使用扣除费用、滑点、资金费率和冲击成本后的净收益。

---

## 四、详细设计

### 4.1 回测可信账本设计

目标：让回测结果能用于策略比较、模型晋级和风控评估，而不是只作为展示结果。

| 对象 | 设计要求 |
|------|----------|
| `cash` | 表示可用现金，平仓后的已实现盈亏已经反映在 cash 中 |
| `realized_pnl` | 用作累计归因字段，不应在总权益计算中再次与 cash 相加 |
| `unrealized_pnl` | 只来自未平仓仓位的 mark-to-market |
| `fee` | 以订单成交额为基础计算，并在发生时扣减 |
| `slippage` | 每笔成交保留 bps 与金额，进入绩效报告 |
| `margin` | 当前为 1x notional 简化模型；杠杆保证金应作为后续独立模型 |

必备测试：开多/开空、同向加仓、部分平仓、完全平仓、反手开新方向、手续费与滑点、杠杆保证金、随机成交序列资金守恒。

回测引擎通过以下条件前，不允许作为“自动晋级实盘”的依据：撮合测试覆盖全部订单生命周期；绩效指标测试覆盖净收益、回撤、费用、滑点；任意成交序列不得产生负数量仓位、重复记账或权益不守恒。

### 4.2 绩效与归因设计

| 类别 | 指标 | 当前状态 |
|------|------|----------|
| 收益 | 总收益、年化收益 | 已有 |
| 风险 | 最大回撤、回撤曲线 | 已有 |
| 风险调整收益 | Sharpe、Sortino、Calmar | 部分已有 |
| 成本 | 手续费、滑点、资金费率、冲击成本 | 手续费已有，滑点金额缺失 |
| 尾部风险 | VaR、CVaR、最大单笔亏损 | 未完成 |
| 基准比较 | Benchmark return、Alpha、Beta | 未完成 |
| 稳定性 | 回撤持续期、月度胜率、滚动 Sharpe | 未完成 |
| 校准 | Brier Score、Log Loss、校准曲线 | 未完成 |

实现要求：`SimulatedFill` 或归因对象应保存 `slippage_cost`；`PerformanceReport` 应扩展 `alpha`、`beta`、`var_95`、`cvar_95`、`max_drawdown_duration`；API 默认返回净收益，毛收益仅作为调试字段；每个指标必须有可手算样本的单元测试。

### 4.3 数据与特征仓库设计

最新代码已经新增 `feature_definitions`、`feature_values`、`market_regimes`、`ohlcv_daily`，并提供 `/features/definitions`、`/features/values`、`/features/regimes/*` API。

下一步需要补齐：

| 能力 | 要求 |
|------|------|
| 数据血缘 | 每个特征值记录数据源、源时间范围、计算版本、参数 hash |
| 数据质量 | freshness、缺口率、异常值率、回填状态 |
| 长期回填 | 至少 2 年 OHLCV，优先 BTC/ETH/SOL 与高流动性 USDT 永续 |
| 高频数据 | 逐笔成交、主动买卖量、订单簿快照、深度不平衡 |
| 衍生品数据 | 资金费率、持仓量、多空比、清算、基差 |
| 跨市场数据 | 多交易所价差、现货-永续-交割合约价差 |
| 事件数据 | 新闻、宏观、监管、链上事件结构化 |

特征分类：动量、波动率、成交量、微观结构、资金与基差、市场状态。当前 MVP 已覆盖 RSI、MACD、SMA、EMA、ATR、Bollinger width、realized volatility、return、volume ratio、funding rate、market regime；后续补 ADX、Donchian、VWAP、OBV、MFI、CVD、spread、depth imbalance、basis、OI change。

### 4.4 概率信号与决策卡设计

预测目标不应是“价格是多少”，而应输出给定周期的条件分布：

```text
r(t,h) = ln(P(t+h) / P(t))
```

模型输出：`p_up`、`p_down`、`p_flat`、`q10/q50/q90`、`expected_volatility`、`mae_estimate`、`uncertainty`。

净期望：

```text
EV = p_up * mu_up + p_down * mu_down + p_flat * mu_flat - fee - slippage - funding - impact
```

交易必须同时满足：`EV > EV_min`、`P(EV > 0) > p_min`、`CVaR_95 < risk_budget`、`data_freshness == pass`。

决策卡字段：建议动作、预测周期、概率分布、收益区间、净期望、最坏情形、仓位建议、适用状态、支持证据、反对证据、样本表现、数据血缘。

### 4.5 组合与风控设计

禁止继续使用“最大仓位 × 主观置信度 × 波动率调整”作为最终仓位。目标仓位应由以下约束共同决定：单笔风险预算、Fractional Kelly、波动率目标、相关性约束、流动性约束、组合回撤约束。

| 门禁 | 触发后动作 |
|------|------------|
| 数据过期 | 禁止新开仓，只允许减仓和平仓 |
| Agent 高分歧 | 降低仓位或观望 |
| 市场状态不适用 | 信号降级 |
| 净期望不足 | 禁止交易 |
| CVaR 超预算 | 禁止交易 |
| 连续止损 | 暂停策略 |
| 当日亏损超限 | 触发紧急停止 |

### 4.6 执行与审计设计

执行层必须支持幂等订单 key、outbox 事件表、订单状态机、审计日志、影子模式、模拟盘门禁和断路器。

订单状态机：

```text
created -> risk_checked -> submitted -> partially_filled -> filled/cancelled/rejected
```

审计日志必须回答：谁、何时、基于什么证据、通过什么规则生成和执行订单。

---

## 五、实施路线图

### 第一阶段：可信底座

| 任务 | 状态 | 下一步 |
|------|------|--------|
| 修复撮合与账户记账 | 已完成 | 增加杠杆和随机序列测试 |
| 建立资金守恒、反手、费用测试 | 部分完成 | 将滑点金额纳入报告测试 |
| 修复报告数据隔离 | 已完成 | 评估是否增加 PostgreSQL RLS |
| 接通已有测试 | 已完成 (v1.3) | `cargo test --test agents` 22 个测试通过，可作为门禁 |
| 修正 Sharpe、Sortino、Calmar | 部分完成 | 补 CVaR、Alpha/Beta、回撤持续期 |
| 暂停不可信回测自动晋级实盘 | 已完成 (v1.2) | 在晋级流程加入回测可信等级门禁 |

验收标准：`cargo test` 覆盖撮合、绩效、报告权限和特征计算；滑点成本不再固定为 0；测试资产没有 HTML 转义或无效 async 用法；回测报告明确标记“可用于展示/可用于比较/可用于晋级”的可信等级。

### 第二阶段：量化基础

| 任务 | 状态 | 下一步 |
|------|------|--------|
| 建立特征仓库 | 已启动 | 增加血缘字段和数据质量字段 |
| 建立长期 OHLCV | 已启动 | 增加回填任务和覆盖率报表 |
| 建立市场状态模型 | 已启动 | 做样本外验证和状态内绩效归因 |
| 增加微观结构与衍生品数据 | 未完成 | 设计采集表与采集任务 |
| 建立概率校准 | 未完成 | 增加 Brier/LogLoss/校准曲线 |
| Walk-forward 验证 | 未完成 | 增加训练、验证、测试时间切片 |

验收标准：主要交易对历史数据覆盖超过 2 年；每个信号都能追溯到特征版本和数据时间；样本外表现优于简单基准，并按市场状态分组展示。

### 第三阶段：组合与资金管理

任务：实现 Fractional Kelly 与波动率目标仓位；增加相关性、风险贡献、CVaR 和流动性约束；统一模拟、回测和实盘执行模型；将 Agent 置信度降级为证据输入，不直接决定仓位。

验收标准：每个仓位建议都能解释风险预算来源；组合最大回撤、单笔亏损和相关暴露透明可追溯；模拟盘与实盘执行结果可对账。

### 第四阶段：可信产品体验

任务：上线概率决策卡；展示数据血缘、证据时间线和模型卡；展示校准曲线、历史同类场景和反事实解释；自动生成交易后归因和策略失效提醒。

验收标准：每笔交易都有完整证据链；用户可以看到“为什么做、何时证明错误、为何退出”；数据过期、模型失配、Agent 分歧时明确提示“暂不建议交易”。

---

## 六、API 与数据设计补充

### 已有 API

| API | 用途 | 状态 |
|-----|------|------|
| `GET /features/definitions` | 查询特征定义 | 已有 |
| `GET /features/values` | 查询特征值时间序列 | 已有 |
| `GET /features/regimes/history` | 查询市场状态历史 | 已有 |
| `GET /features/regimes/latest/{symbol}` | 查询最新市场状态 | 已有 |
| `POST /features/regimes/aggregate-daily` | 触发日 OHLCV 聚合 | 已有 |
| `GET /features/quality` | 查询数据新鲜度、缺口率、异常值率、覆盖率与质量等级 | 已实现 (v1.3) |
| `POST /signals/decision-card` | 生成概率决策卡 | 已实现 (v1.2) |
| `GET /signals/decision-cards` | 查询用户决策卡列表 | 已实现 (v1.2) |
| `GET /signals/calibration` | 查询概率校准曲线和 Brier/LogLoss | 已实现 (v1.2) |
| `POST /signals/calibration/compute` | 触发校准计算 | 已实现 (v1.2) |
| `GET /backtest/jobs/{job_id}/trust-level` | 查询回测可信等级 | 已实现 (v1.2) |
| `POST /backtest/jobs/{job_id}/trust-level` | 触发回测可信等级评估 | 已实现 (v1.2) |

### 待实现 API

| API | 用途 | 优先级 |
|-----|------|--------|
| `POST /features/backfill` | 触发历史数据与特征回填 | P1 |
| `GET /risk/portfolio-exposure` | 查询组合风险暴露与相关性 | P1 |

### 已实现表（018 迁移）

| 表 | 用途 | 状态 |
|----|------|------|
| `data_quality_snapshots` | 数据新鲜度、缺口率、异常率、覆盖率 | 已实现 |
| `feature_lineage` | 特征血缘、参数 hash、计算代码版本 | 已实现 |
| `signal_predictions` | 概率预测、分位数、模型版本 | 已实现 |
| `signal_calibration_reports` | 校准曲线、Brier、LogLoss、样本量 | 已实现 |
| `decision_cards` | 用户可见决策卡快照 | 已实现 |
| `risk_decisions` | 风控门禁结果与拒绝原因 | 已实现 |
| `execution_audit_logs` | 执行审计日志 | 已实现 |
| `backtest_trust_assessments` | 回测可信等级评估 | 已实现 |

---

## 七、测试与发布门禁

| 层级 | 范围 | 必须覆盖 |
|------|------|----------|
| 单元测试 | 撮合、绩效、特征计算、风控公式 | 可手算样本 |
| 集成测试 | API、数据库权限、特征查询 | 用户隔离、时间范围、分页 |
| 属性测试 | 随机成交序列 | 资金守恒、无负仓位、无重复记账 |
| 回归测试 | 固定历史窗口 | 指标结果稳定 |
| 样本外验证 | Walk-forward | 概率校准和基准对比 |

任何实盘相关能力发布前必须满足：后端测试通过；前端生产构建通过；数据隔离测试通过；回测可信等级达到“可用于比较”以上；影子模式连续运行满足最小样本量；决策卡显示净期望、风险预算、数据血缘和失效条件。

---

## 八、关键代码位置索引

| 主题 | 文件路径 |
|------|----------|
| 撮合引擎 | `backend/src/backtest/matching_engine.rs` |
| 绩效指标 | `backend/src/backtest/performance_engine.rs` |
| 回测模型 | `backend/src/backtest/models.rs` |
| 回测运行器（收敛到编排层） | `backend/src/backtest/runner.rs` |
| 决策编排器 | `backend/src/orchestration/decision_orchestrator.rs` |
| 账本服务（统一交易入口） | `backend/src/orchestration/ledger_service.rs` |
| 组合风险管理 | `backend/src/backtest/portfolio_risk.rs` |
| 回测可信等级 | `backend/src/backtest/trust_engine.rs` |
| 特征仓库迁移 | `backend/migrations/017_feature_store_and_regime.sql` |
| 量化基础迁移 | `backend/migrations/018_quantitative_foundations.sql` |
| Reports 迁移安全转换 | `backend/migrations/030_reports_schema_fix.sql` |
| 特征计算 | `backend/src/features/calculator.rs` |
| 特征存储（含血缘/质量） | `backend/src/features/store.rs` |
| 市场状态识别 | `backend/src/features/regime.rs` |
| 特征 API | `backend/src/routes/features_api.rs` |
| 概率信号模块 | `backend/src/signals/mod.rs` |
| 决策卡数据模型 | `backend/src/signals/models.rs` |
| 概率校准 | `backend/src/signals/calibration.rs` |
| 信号存储 | `backend/src/signals/store.rs` |
| 信号 API | `backend/src/routes/signals_api.rs` |
| 回测 API（含可信等级） | `backend/src/routes/backtest_api.rs` |
| 报告权限隔离 | `backend/src/routes/reports.rs` |
| Agent 辩论 | `backend/src/agents/debate.rs` |
| Agent 主观评分 | `backend/src/agents/agents.rs` |
| AI 分析大路由 | `backend/src/routes/ai_analysis.rs` |
| 前端 Signal API 封装 | `frontend/src/api/index.ts` |
| 前端概率决策卡页面 | `frontend/src/pages/DecisionCardPage.vue` |
| 前端回测中心页面 | `frontend/src/pages/BacktestPage.vue` |
| 前端路由配置 | `frontend/src/router/index.ts` |
| 前端侧边栏菜单 | `frontend/src/layouts/DashboardLayout.vue` |

---

## 九、下一步优先级

短期最应该做的不是继续增加 Agent，而是按以下顺序收口：

1. ~~补齐绩效报告的滑点成本、CVaR、Alpha/Beta 和回撤持续期。~~ **已完成 (v1.2)**
2. ~~清洗并接入 `backend/tests/agents/*.rs`，建立测试门禁。~~ **已完成 (v1.3)**
3. ~~给特征仓库补数据质量与血缘字段。~~ **已完成 (v1.2)**
4. ~~建立概率决策卡的后端数据结构。~~ **已完成 (v1.2)**
5. ~~在自动晋级或实盘授权流程中加入回测可信等级和影子模式门禁。~~ **已完成 (v1.2)**
6. ~~把 runner 改成统一调用 DecisionOrchestrator。~~ **已完成 (v1.7)**
7. ~~把 runner / 模拟盘 / 实盘统一改走 LedgerService。~~ **已完成 (v1.7)**
8. ~~给 reports 迁移加历史非 JSON 内容的兼容处理。~~ **已完成 (v1.7)**
9. ~~Portfolio Risk 接入 runner 主链路（correlation/portfolio_cvar/liquidity）。~~ **已完成 (v1.7)**
10. ~~EV/CVaR 模型增强：概率校准、funding_rate、CVaR sqrt(t) 效应。~~ **已完成 (v1.8)**
11. ~~产品信任闭环：DecisionCard 补 reasons/blockers/trust_level 字段并前端展示。~~ **已完成 (v1.8)**

P0 + P1 核心项全部收口。后续 P2 候选：
- `POST /features/backfill`（历史数据回填）
- `GET /risk/portfolio-exposure`（组合风险暴露查询 API，引擎已接入 runner）
- 校准数据从 DB 自动读取到 DecisionInput.calibration（当前需手动传入）
- orderbook liquidity / cross-exchange spread 接入成本计算
- 杠杆保证金与随机序列资金守恒属性测试
- 模拟盘/实盘也改走 Orchestrator + LedgerService（当前仅回测路径已收敛）

一句话判断：**项目方向正确，最新代码已从"AI 辅助分析"走向"可验证量化基础"，且回测主链路已收敛到统一编排层（Orchestrator + LedgerService + PortfolioRisk），EV/CVaR 模型已接入概率校准和资金费率，决策卡信任闭环已贯通后端到前端。当前实现上仍应优先把模拟盘/实盘也改走统一编排层、校准数据从 DB 自动读取、orderbook/cross-exchange 数据接入。**
